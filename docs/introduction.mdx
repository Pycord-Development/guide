im0_arr = np.array(img0)
gray0 = np.mean(im0_arr, axis=2)
# The background is very dark, let's look for pixels with brightness > 40
logo_pixels = np.argwhere(gray0 > 40)
if len(logo_pixels) > 0:
    ymin0, xmin0 = logo_pixels.min(axis=0)
    ymax0, xmax0 = logo_pixels.max(axis=0)
    print(f"Logo bounding box (threshold 40): ymin={ymin0}, xmin={xmin0}, ymax={ymax0}, xmax={xmax0}")
else:
    print("No logo pixels found above threshold")
